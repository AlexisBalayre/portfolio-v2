# Next.js / Node
.next/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Yarn Berry
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
# Never commit unplugged/cache (can contain large native binaries)
.yarn/unplugged/
.yarn/cache/
.pnp.*

# OS/editor junk
.DS_Store

# Claude Code local overrides (copy .claude/settings.local.json.example to start)
.claude/settings.local.json

# Git worktrees created by `yarn worktree:create`
.worktrees/

# Env / secrets
.env
.env.*
!.env.example

# TypeScript incremental build state (rewritten by every tsc run)
tsconfig.tsbuildinfo
